---
tags: [inventory, backend, repo-root]
zone: packaging
---
# 10 · Build, packaging, distribution & repo root

Back to [[00 Backend inventory]] · Unused files: [[99 Unused files]]

**Scope:** everything that turns the repo into something a user runs:
- the Python package metadata
- the committed frontend build in `panoptic/html/`
- Docker, install scripts and CI

The repo-root files are listed here because they all package or launch the backend; the frontend ships *inside* `panoptic/html/`.

## Zone-level checks
- [ ] ⚠ **The package can't be built right now.** `pyproject.toml` has `[tool.hatch.version] path = "panoptic/__init__.py"`, but that file has been empty since `834bf69e` (2026-06-04). `uv run` fails with `Error getting the version from source regex: unable to parse the version from the file: panoptic/__init__.py`. That also breaks `pip install .`, both Dockerfiles (`pip3 install .`) and `hatch-build-and-publish.yml`. Restoring `__version__ = "…"` fixes it.
- [ ] ⚠ Dependencies in `pyproject.toml` have drifted from the code:
  - **imported but not declared:** `msgspec` (14 files), `click` (`cli.py`), `httpx` (IIIF reader, project routes)
  - **declared but never imported:** `aiofiles`, `aiosqlite`, `fastapi-camelcase`, `pypika`, `pendulum`, `imagehash`, `show-in-file-manager`
  - Keep `python-multipart`: it's needed for `UploadFile` / `Form` in `project_routes.py`.
- [ ] `pyproject.toml` classifiers list Python 3.9, but `requires-python = ">=3.10"`.
- [ ] ⚠ `panoptic/html/` is **stale**. A fresh `npm run build` on 2026-09-11 produced different hashes for `index-*.js`, `MainView-*.js/css`, `HomeView-*.js`, `NotifModal-*.js`, `SandboxView-*.js` and `TestView-*.js`. Rebuild before any release.
- [ ] `panoptic/html/` ships the dev-only routes `/#/test` (SandboxView) and `/#/test-points` (TestView), plus 5 icons nothing references ([[99 Unused files]]).
- [ ] ⚠ `docker/gpu/Dockerfile-panoptic` line 11 is `pip3 install --force-reinstall` with **no package argument**, which makes pip fail. `docker-publish.yml` builds it.
- [ ] `.github/workflows/build-base.yml` references a `Dockerfile-base` that doesn't exist ([[99 Unused files]]).
- [ ] No CI workflow runs the tests or the frontend build.

## Files: Python package
- [ ] `pyproject.toml`. Hatchling build, dependencies, the `panoptic` script, wheel artifacts (`migration/schema/*.sql`), excludes `migration/tests`.
- [ ] `uv.lock`. Lockfile for `uv run` / `uv sync`.
- [ ] `.gitignore` (backend). Only `test/scripts/*.db`, but `test/scripts/tmp.db` is tracked anyway.

## Files: committed frontend build (`panoptic/html/`, generated by `cd panoptic_front && npm run build`)
Served by `main.py` at `/`. Regenerate these; don't edit them.
- [ ] `panoptic/html/index.html`
- [ ] `panoptic/html/favicon.ico`
- [ ] `panoptic/html/icons/iiif.svg`. The only icon the UI uses (`FolderPanel.vue`).
- [ ] `panoptic/html/assets/index-BbiRYSws.js` + `index-D7YqdquY.css`. App entry chunk.
- [ ] `panoptic/html/assets/HomeView-AG-2xMe5.js` + `HomeView-CUqauXq9.css`
- [ ] `panoptic/html/assets/MainView-DoDfX60V.js` + `MainView-N3KkCJ6B.css`
- [ ] `panoptic/html/assets/NotifModal-1dTtWhWm.js` + `NotifModal-2JpHHuYm.css`
- [ ] `panoptic/html/assets/SandboxView-Cn7qmLWM.js` + `SandboxView-0WR5u5lA.css`. Dev route.
- [ ] `panoptic/html/assets/TestView-DLrRT36h.js` + `TestView--GaB1Qen.css`. Dev route.
- [ ] `panoptic/html/assets/three.module-Drl2D0eQ.js`. three.js chunk for the map view.
- [ ] `panoptic/html/assets/_plugin-vue_export-helper-CSBScHBl.js`
- [ ] `panoptic/html/assets/InterVariable-DiVDrmQJ.woff2` + `InterVariable-Italic-FCBEiFp6.woff2`
- [ ] `panoptic/html/assets/bootstrap-icons-BeopsB42.woff` + `bootstrap-icons-mSm7cUeB.woff2`
- [ ] `panoptic/html/assets/duck-S9Uqeb_r.svg`. Easter-egg image (`tutorials/Egg.vue`).

## Files: repo root (paths relative to the repo root)
- [ ] `README.md` / `README-FR.md`. Install docs; they point to `install/start_panoptic_*.sh` and `install/windows/panoptic.exe`.
- [ ] `LICENSE` (MPL-2.0) · `CITATION.cff`
- [ ] `.gitignore` (root)
- [ ] `PluginSimpleGuide.md`. Plugin author guide ([[07 Plugin system]]).
- [ ] `DATAFLOW.md`, `DATAFLOW.svg`, `DATAFLOW.png`. Data-flow poster. The SVG is generated by `scripts/gen_dataflow_svg.py`.
- [ ] `scripts/gen_dataflow_svg.py` · 232 L. Writes `DATAFLOW.svg`.
- [ ] `scripts/create_fake_data.py` · 44 L. Manual: reads an export `data.csv` and writes a big fake `data_big.csv` for load tests.

## Files: Docker
- [ ] `docker/cpu/Dockerfile-panoptic`. `pip3 install .` from `panoptic_back` on top of `ceressorbonne/panoptic-clip-base`.
- [ ] `docker/cpu/Dockerfile-panoptic-clip-base`. python:3.12 slim base.
- [ ] `docker/gpu/Dockerfile-panoptic`. ⚠ broken `pip3 install --force-reinstall` line.
- [ ] `docker/gpu/Dockerfile-panoptic-clip-base`. pytorch CUDA base.
- [ ] `docker/entrypoint.sh`. Optional `pip install -U panoptic panopticml`, then `exec panoptic`.

## Files: installers
- [ ] `install/start_panoptic_linux.sh` · 71 L. uv-based Linux launcher (in the README).
- [ ] `install/start_panoptic_mac.sh` · 135 L. macOS launcher (in the README).
- [ ] `install/windows/panoptic_launcher_uv.py` · 65 L. Source of the Windows launcher.
- [ ] `install/windows/panoptic.exe`. Built Windows launcher (in the README).
- [ ] `install/windows/panoptic.ico`

## Files: GitHub
- [ ] `.github/workflows/docker-publish.yml`. Builds and publishes the Docker images.
- [ ] `.github/workflows/hatch-build-and-publish.yml`. PyPI publish on `v*.*.*` tags.
- [ ] `.github/workflows/test-install-all-platforms.yaml`. Manual install test on 3 OSes.
- [ ] `.github/ISSUE_TEMPLATE/bug-.md`
- [ ] `.github/ISSUE_TEMPLATE/feature_request.md`
- [ ] `.github/ISSUE_TEMPLATE/question.md`
