
The connection worklow should look like this

The Panoptic connection
keeps track of the global instance. 
existing projects, users, plugins. 
able to load projects.

Is managed by panoptic store.

The project connection
full rest to get data
can get update signals from the sync server
only exception is the tasks states that can be fed from socket io.
